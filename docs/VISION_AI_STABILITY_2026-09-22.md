# Vision AI stability handoff — 2026-09-22

## Scope and code

- Repository: `weilu-kong/Full-Function-S31-P4-Demo`; branch: `codex/vision-ai`.
- Hardware: ESP32-S31 / Korvo-1; project: `firmware/korvo1_yokai_demo`.
- The diagnostic/fix sequence started from `daabee0`. Commits `ddf0780`, `3ae51e3`, `54d82a0`, `6c6d73c`, `0a804b2`, and `d17ed9a` add UI/preview telemetry, guarantee inference-buffer release, reserve the preview write buffer, sequence frame publication, snapshot health timestamps, and fix display triple-buffer synchronization.
- The managed-component fix is reproduced by `firmware/korvo1_yokai_demo/tools/apply_managed_component_patches.py` after dependency resolution. It registers `pending_fb` and old-buffer ownership **before** starting a panel switch in the normal and dummy-draw LVGL v9 `TRIPLE_FULL` paths.

## Root cause and verification

The diagnostic firmware reproduced a home-screen UI freeze at about 448 s. The independent health task kept running, but UI enter/exit stopped at `11991/11991`. `[DISPLAY_STALL]` showed a pending framebuffer with `frame_complete=11992` and `switch_seq=11991`: the frame-complete callback had run before `pending_fb` was registered, so the LVGL worker waited indefinitely for a switch notification that had already passed. Camera, inference, and heap loss were not required to trigger this freeze.

- Host Vision unit test passed, the managed-component patch check passed, and the ESP-IDF app build passed. The app image was `0x9aeef0` bytes with 16% of the smallest app partition free.
- The fixed app was flashed at 921600 baud; esptool verified the written-data hash.
- Home screen: approximately 632 s; UI enter/exit reached `16965/16965`, with no observed `DISPLAY_STALL`, WDT, or reboot.
- Vision screen: entered at device uptime about 67 s and remained active past uptime 1889 s (over 30 minutes in Vision). Final `cap/pub/infer=28488/28488/28482`, UI enter/exit `13167/13167`, `cam_err=0`, `no_free_buf=0`. The monitored logs showed no persistent preview/display stall, WDT, or reboot. The serial monitor was reattached once with `--no-reset`; telemetry and counters continued across that short observation gap.

## Memory interpretation and risk

`int_free` and `psram_free` are **remaining free heap bytes**, not bytes in use. In the steady Vision state, `int_free` was about **68,295 B (66.7 KiB)** and `psram_free` was generally about **492,000 B (480 KiB)**. The largest compatible PSRAM block was **450,560 B (440 KiB)**. Current free values did not decline monotonically during the 30-minute run; occasional PSRAM dips to about 485 KB recovered. Recorded low-water marks were `int_min=29,484 B` and `psram_min=436,484 B`; these are historical minima, not current free space.

This is **tight headroom**, not an all-clear: the device has 16 MB physical PSRAM, but most is committed to the UI, model/detector, camera, and preview buffers. A new allocation larger than the largest free contiguous block can fail even while aggregate PSRAM free is about 492 KB. In particular, the earlier MFN recognition memory gate (at least 900 KiB contiguous) is **not met** in this steady Vision state. Do not infer that enrollment/recognition memory feasibility passed from the preview soak.

## Remaining plan / acceptance gates

### Phase 5/6 bring-up execution — 2026-09-22

- Baseline confirmed: clean `codex/vision-ai` worktree at `095def56302237ad3edfcae21c40fa80817b9f8c` (display fix parent `d17ed9a`). Managed-component patch `--check` and host Vision test passed.
- ESP-IDF 6.2.0 (`esp-idf-master`) full build passed. App image: `0x9aeef0` bytes (10,153,712 bytes); smallest app partition has 16% free.
- Connected board identified as ESP32-S31 revision 0 on `/dev/cu.usbserial-1120`. Fresh baseline image, bootloader, partition table, and SR model image written with `FLASH_BAUD=920160`; esptool reported `Hash of data verified` for all four segments. `FLASH_VERIFY=PASS`.
- Post-flash HOME serial sample at uptime 136–161 s: UI enter/exit counters advanced from 3638/3638 to 4176/4176; Vision state OFF; 21 tasks; `int_free=86199`, `int_largest=31744`, `psram_free≈4,479,300`, `psram_largest=4,456,448`, `simd_largest=4,456,448`; `cam_err=0`, `no_free_buf=0`. No `DISPLAY_STALL` or WDT appeared in this short sample. Boot/reset reason was not captured.
- Stage 1 20-cycle touchscreen test and all later hardware gates remain **pending**. This HOME sample does not establish preview, MFN, recognition, enrollment, or soak acceptance.
- Operator reported completing 20 HOME ↔ Vision cycles. Post-cycle HOME telemetry at uptime 1821–1838 s showed 21 tasks, `cam_err=0`, `no_free_buf=0`, `cap/pub/infer=773/773/640`, UI heartbeats still advancing, `int_free=84999`, and `psram_free≈499,000`. The serial monitor was not attached during those cycles, so per-cycle memory trends, frame recovery, and intermittent stalls cannot be certified. A new continuous serial capture was started for the repeat acceptance run. The lower HOME PSRAM is consistent with retained preview buffers, camera MMAP buffers, and the face detector; it is not, by itself, proof of a leak.
- Repeat Stage 1 continuous capture recorded 20 complete Vision starts, camera starts, capture/inference task exits, camera stops, and Vision stops. Vision dwell times were approximately 4.6–12 s. `cap/pub/infer` advanced from `773/773/640` to `2868/2868/2523`; `cam_err=0`, `no_free_buf=0`, `DISPLAY_STALL=0`, with no WDT or reboot text in the capture. Task count returned from 23 in Vision to 21 at HOME. Pre-entry PSRAM free was 498,640 B initially and 498,352 B before the final cycle; largest compatible block remained 458,752 B. Final HOME PSRAM recovered to about 499 KB. Operator confirmed touch/UI responsiveness. **Stage 1 PASS** for this flashed baseline; MFN contiguous-memory gate remains blocked.
- Stage 2 diagnostic app (`0x9af170` bytes) was built and flashed at 920160 baud; the app's written-data hash verified. Cold boot reset reason was `POWERON`. M0 boot before Vision init: `psram_largest=12,582,912`; after three preview buffers M2: `12,320,768`; UI ready/M1 before Vision: `4,456,448`; two camera MMAP buffers were each 1,843,200 B; M4 after camera init: `770,048`; M5 after STREAMON: `770,048`; M6 after detector construction: `770,048`; M7 after first face detection: `458,752`. M12 after STREAMOFF and M14 after leaving Vision remained `458,752` B. This confirms camera MMAP allocation and first detector run as the major contiguous-memory drops. STREAMOFF alone did not release the camera buffers. M8–M11 require an enrollment/recognition trigger; M13 requires the separate deinit experiment.
- Stage 3 C independent deinit experiment (`0x9af270` bytes) was built, flashed at 920160 baud, and hash-verified. M12 after STREAMOFF: largest compatible block 450,560 B; M13 after deinit/munmap: 3,670,016 B. On the next Vision entry, V4L2 again negotiated 1280×720 UYVY and `VIDIOC_REQBUFS` failed (`esp_video: Failed to create buffer`), leaving Vision in error state. Thus deinit releases the buffers but the current full-resolution camera cannot restart in the remaining contiguous layout. The experiment's deinit call was removed; no MFN load was attempted. Native 320×240 capture is the next preferred investigation.
- A separate 640×480 UYVY `VIDIOC_S_FMT` experiment (`0x9af260` bytes) was built, flashed at 920160 baud, and hash-verified; V4L2 rejected it before buffer allocation (`dvp_video: input width=640, height=480 is not supported`). Boot logs identified the actual sensor as **SC101IOT PID 0xda4a**, not the OV3660 mentioned in the BSP documentation. The enabled SC101IOT driver formats are 1280×720 UYVY only, and the DVP set-format implementation rejects any width/height other than the current sensor format. Thus the existing driver has no native 320×240 or 640×480 path. The failed format request was removed and the known working diagnostic app was restored. Continue with MFN allocation-order investigation rather than assuming OV3660 capabilities apply to this board.
- HumanFaceRecognizer 0.3.2 source confirms `lazy_load=true` leaves the MFN model and feature database unallocated in the constructor; the first feature operation loads them. Eager construction before the two 1,843,200 B camera buffers may still leave insufficient total PSRAM for camera allocation. The next independent experiment requests one V4L2 MMAP buffer while retaining all three preview buffers; it must pass camera/preview stability before MFN is attempted.
- The one-camera-buffer experiment (`0x9af100` bytes) was built, flashed at 920160 baud, and hash-verified. It raised largest compatible PSRAM to 2,621,440 B after STREAMON, but the first capture triggered `esp_cache_msync(96): null pointer`, followed by `abort()` and `SW_CPU_RESET`. This configuration is **rejected**; camera buffer count was restored to two. No MFN load was attempted on this image.
- Final state of this bring-up session: rebuilt the two-camera-buffer Stage 2 diagnostic app (`0x9af170` bytes), flashed it at 920160 baud, and verified the written hash. HOME serial telemetry at uptime 34–45 s showed 21 tasks, matched UI enter/exit heartbeats, `int_free=86199`, `psram_free≈4.48 MB`, `psram_largest=4,456,448`, `cam_err=0`, and `no_free_buf=0`. This final reflash has not yet had a fresh Vision interaction test. **Phase 5 remains implemented but blocked by the MFN contiguous-memory gate; Phase 6 remains implemented with hardware acceptance pending.** Do not start Phase 7.
- The final restored two-buffer image subsequently passed a fresh two-cycle HOME ↔ Vision smoke test. The operator confirmed preview, face boxes, and touch were normal. Serial log recorded two starts and two complete stops, `cap/pub/infer=915/915/851`, `cam_err=0`, `no_free_buf=0`, no `DISPLAY_STALL`, WDT, or error lines; tasks returned from 23 in Vision to 21 at HOME, and PSRAM free recovered to about 497 KB. This verifies restored preview behavior, but does not lift the MFN gate or count as Phase 5/6 acceptance.

1. Keep the current telemetry while testing 20 Vision enter/exit cycles, including touch/UI responsiveness and stable heap after returning home.
2. Test enrollment failure, cancel, deletion/re-enrollment, and reboot recovery. Re-measure contiguous PSRAM before loading MFN. Only attempt MFN when its memory gate passes; if necessary, test loading it before camera allocation or while camera is stopped, then restart camera and re-check stability.
3. Exercise voice commands concurrently with Vision, then run a longer soak with screen transitions and interaction. The 30-minute result covers a mostly stationary Vision page, not all interaction paths.
4. Treat a future stall as a new observation: compare UI enter/exit, `cap/pub/consumed/infer`, buffer indices, `DISPLAY_STALL`, and heap before changing code. Do not add LVGL refresh throttling or image-descriptor changes without evidence they address a reproduced failure.

The original detailed debug plan is the local document `vision_preview_stall_debug_fix_plan.md`; this handoff records the executed phases and outstanding gates without copying that historical 1594-line plan into the repository.

## MFN-MEM-01 — DOUBLE_FULL experiment

- Changed only the LCD tear avoidance mode to `DOUBLE_FULL`, added `[MEMCFG]` and M1/M4/M7 allocator summaries, and built the app (`0x9af300` bytes, 16% app partition free). Managed patch check, host Vision test, and UI source regression check passed. Flashed at 920160 baud; esptool verified the written-data hash.
- The operator reported one crash/reboot when a face appeared after the first restart. The serial capture did not cover that incident, so its reset reason is unknown. On a subsequent run, preview and face detection worked.
- Captured M7 after face detection: `psram_free=492056`, `psram_largest=417792`, `simd_largest=417792` bytes. The compatible largest block is 503808 bytes below the 921600-byte MFN gate; total free is also below the gate. This is **FAIL-A (insufficient resident capacity)**. No enrollment or MFN load was attempted.
- The subsequent serial capture covered about 8 minutes of Vision and a return to HOME: `cap/pub/infer=9170/9170/9085`, `cam_err=0`, `no_free_buf=0`, with no recorded `DISPLAY_STALL`, WDT, or reset. M4 and the boot `[MEMCFG]` line fell outside that capture; M1 and M7 were recorded.
- `DOUBLE_FULL` was reverted to the stable `TRIPLE_FULL` mode. The memory telemetry and a corrected `[MEMCFG]` log remain for the next isolated experiment. Investigate resident display/LVGL allocation before allocation-order experiments; do not attempt MFN while total free remains below 921600 bytes.

## MFN-MEM-02 — physical LCD framebuffer count

- Root cause of MFN-MEM-01's missing memory gain: `CONFIG_BSP_LCD_RGB_BUFFER_NUMS=3` made the BSP allocate three full 800×480 RGB565 framebuffers before LVGL selected `DOUBLE_FULL`. The mode switch alone could not release one of those BSP allocations.
- In this isolated experiment, set the BSP framebuffer count to 2 and LVGL mode to `DOUBLE_FULL`; camera MMAP remains 2 and preview buffers remain 3. Managed patch check, Vision host test, UI source regression check, and full ESP-IDF build passed. Generated `sdkconfig.h` confirms `CONFIG_BSP_LCD_RGB_BUFFER_NUMS 2`; image is `0x9af300` bytes with 16% app partition free. Flashed at 920160 baud; write hash verified.
- Fresh boot logged `[MEMCFG] lcd=DOUBLE_FULL lcd_fb=2`. HOME-ready PSRAM free was 5,251,052 B versus about 4,481,228 B on the restored three-buffer firmware, a gain of about 770 KB.
- First Vision M1: free 5,248,040 B, largest compatible 5,111,808 B. M4 after two camera MMAP buffers: free 1,561,212 B, largest 1,540,096 B. M7 after first face detection: free 1,256,404 B, largest compatible 1,212,416 B. **Memory gate passes** (threshold 921,600 B); functional/display acceptance and MFN load remain pending.
- Operator confirmed preview, face boxes, and touch are normal. Enrollment of `kdd` succeeded: five samples accepted (feature IDs 1–5), metadata generation 2 saved, and person slot 0 committed. First MFN operation reduced free PSRAM from 1,247,980 B at M10 to 331,868 B at M11 (observed delta 916,112 B); largest compatible block fell from 1,212,416 B to 278,528 B. No allocation abort, WDT, or camera error was observed in that capture.
- Operator found missing glyphs in the management row (`特徴量` used characters absent from the bundled 16px font) and slow name entry. Replaced the row text with supported `5 件保存済`; while the enrollment or management modal is visible, the UI continues consuming preview/results but suppresses background preview and label redraws. Hardware validation of these UI changes is pending.
- The UI fix image (`0x9af390` bytes, 16% app partition free) built and was flashed at 920160 baud with hash verification. On reboot, metadata for one person (generation 2) loaded and MFN completed first recognition lazy-load without abort; the operator confirmed the management-row corruption is gone and name entry is still slow but acceptable. The 10-cycle / 10-minute display regression and explicit known-face recognition confirmation remain pending.
- The cycle regression exposed a new crash on the second Vision exit after MFN had loaded: capture task exited, then `esp_cache_msync(96): null pointer` aborted and reset the S31 before inference exited. `vision_service_stop()` had waited for both tasks before STREAMOFF, leaving the DVP stream running without capture returning buffers while the slower MFN inference finished. The stop order was changed to wait for capture exit, STREAMOFF immediately, then wait for inference exit. This fix requires a fresh hardware cycle test before acceptance.
- The stop-order fix image (`0x9af420` bytes, 16% app partition free) built and was flashed at 920160 baud with hash verification. Initial hardware regression recorded three complete Vision enter/exit cycles with the expected order `capture exit → Camera streaming stopped → inference exit`, no `esp_cache_msync` abort, WDT, or display stall. The requested 10-cycle / 10-minute acceptance is still in progress.
- Same-face accuracy diagnostic at the unchanged 0.60 application threshold recorded 44 `[MATCH]` samples: 20 accepted, 15 candidates with scores 0.50–0.60, and 9 with no database candidate above 0.50. The enrolled face was often detected but rejected by the score gate. No unknown-person sample was available, so the threshold was not lowered.
- During attempted re-registration, both five-sample runs failed to replace the existing SPIFFS metadata file (`rename /storage/face_people.tmp → face_people.meta`). The old code had already deleted old features before committing metadata. A second defect in esp-dl `DataBase::query_feat` returned list positions instead of stored feature IDs after deletion, so the second run logged misleading IDs 1–5 while the valid database records were 11–15. The operator was told to stop mutations.
- The 960 KiB storage partition was read-only backed up to `/private/tmp/vision-storage-before-repair.bin` (SHA-256 `e971481d24f86ae6dc44d8ba8ace455d8cc37363be3e70435c596e7b77eac4bc`). The repair uses two alternating, verified metadata files; commits new metadata before deleting old features; reads the actual newly appended feature ID from the database; and patches esp-dl to return stored IDs. A narrowly gated one-person recovery maps the observed old IDs 1–5 to valid IDs 11–15. Host metadata fallback test, managed patch check, and full build passed. Repair image `0x9afab0` was flashed at 920160 baud with hash verification; reboot loaded one person from alternate slot at generation 3. Actual known-face matching and re-registration after repair remain to be validated.
- The operator cannot currently provide an unregistered face for false-accept testing. Keep the 0.60 application threshold unchanged until an unknown-face sample is available. The post-repair UART capture shows metadata generation 3 loaded, but no Vision entry yet; known-face matching is pending operator interaction.
- Post-repair Vision capture subsequently showed stored feature IDs 11 and 13 mapping to the registered person with accepted scores of about 0.62–0.82. Across 48 low-rate diagnostic samples, 32 passed and 16 did not; the operator's face was not continuously confirmed in frame, so this is not a controlled recognition-rate measurement. There was no observed reboot, display stall, or camera error during that capture.
- Removed the one-time board-specific ID recovery code after generation 3 metadata was confirmed. The final app image (`0x9af840`, 16% app partition free) passed host Vision tests, managed-patch check, UI regression, `git diff --check`, and full ESP-IDF build. It was flashed at 920160 baud with hash verification. On the final-image reboot, metadata loaded as one person, generation 3 from the alternate slot; `[MEMCFG]` confirms `DOUBLE_FULL` and 2 LCD framebuffers. Final-image soak and interaction acceptance are in progress.
- Final-image hardware: an initial Vision run lasted about 61 s and exited cleanly (`capture exit → camera stop → inference exit`); a second Vision run passed 10 minutes continuously from device uptime 211 s to 818 s. At the latter point `tasks=23`, `cap/pub/infer=10467/10467/9497`, `cam_err=0`, `no_free_buf=0`, `int_free=71279`, and `psram_free≈341460` B. No reboot, WDT, or `DISPLAY_STALL` was recorded. This is a 10-minute stability checkpoint, not the plan's 30/60-minute final soak.
- During the first final-image Vision run, the operator also registered `xiukoko` as a second entry. Five features (IDs 16–20) were saved and metadata generation 4 committed to the primary slot. The operator has not yet confirmed whether this is a distinct person, so it cannot be counted as multi-person recognition acceptance. A later code review found that power loss between metadata commit and journal cleanup could delete committed features on reboot; journal recovery now skips feature IDs referenced by current metadata. Host tests and a full build of this guard pass; this newer image is not yet flashed while the current soak runs.
- At the operator's stop request, the second Vision run had reached device uptime 1805.7 s, approximately **26 min 35 s continuously** from its start at 211.2 s. Final counters: `cap/pub/infer=25904/25904/24793`, UI enter/exit `12434/12434`, `tasks=23`, `cam_err=0`, `no_free_buf=0`, `int_free=71279`, `psram_free=337608`. The logged free PSRAM fluctuated around 335–342 KB without a sustained decline. No WDT, reboot, or `DISPLAY_STALL` appeared. This does not satisfy the plan's 30-minute recognition or 60-minute Vision soak; the face was not confirmed present throughout. The new power-loss journal guard is built but **not flashed**. Phase 5 still needs cancel/retry, deletion, clear-all, post-fix re-registration and reboot checks, an unknown-face sample, and confirmation that `xiukoko` is a distinct person if counting multi-person behavior.
